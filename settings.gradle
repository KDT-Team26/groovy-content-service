rootProject.name = 'groovy-content-service'

// 공통 코드는 groovy-common 레포로 분리되어 GitHub Packages 로 배포된다.
// (com.groovy.backend:{event-contract,observability,web-common,security-common,client-common,outbox})

include 'services:content-service'
